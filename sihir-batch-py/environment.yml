## This file contains the environment dependencies list prepared by executing: 
# $ conda env export --from-history > environment.yml
#
## Alternatively, detailed results can be extracted using: 
# $ conda env export > environment.yml
name: dev
channels:
  - defaults
dependencies:
  - pymongo
  - python=3
prefix: /home/gsskhan/.conda/envs/dev
